{ name: CI, on: [push], jobs: { build: { name: Build and test, runs-on: ubuntu-latest, defaults: {run: {shell: bash, working-directory: DotnetTemplate.Web}}, steps: [ {uses: actions/checkout@v3 }, {name: build the app, run: dotnet build}, {name: npm dependencies and build, run: npm ci && npm run build }, { name: run dotnet tests, run: dotnet test }, {name: run npm tests, run: npm test }, {name: lint the typescript, run: npm run lint} ] }}}
